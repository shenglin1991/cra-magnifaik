import { Route } from '@angular/router';
import { CraFeatureComponent } from './cra-feature/cra-feature.component';

export const craFeatureRoutes: Route[] = [{ path: '', component: CraFeatureComponent }];
